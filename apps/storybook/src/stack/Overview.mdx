import { Meta, Canvas } from '@storybook/addon-docs/blocks';

import { FIGMA_LIBRARY_URL } from '../docs/links';
import * as CssStories from './Stack.css.stories';

<Meta title='Wrappers/Stack/Overview' name='Overview' />

# Stack

A vertical layout primitive. Children are placed in a flex column with configurable gap
from the `--rgrm-scale-space-*` primitive tokens.

Load `@rgrmdesign/rgrm-ds-tokens` once in your app so the CSS custom properties are
defined.

**Design spec** — <a href={FIGMA_LIBRARY_URL}>View in Figma</a> for anatomy, variants,
and usage notes.

## Preview

<Canvas of={CssStories.GapMd} sourceState='none' />

## Gap variants

### No gap

<Canvas of={CssStories.Default} sourceState='none' />

### Extra small

<Canvas of={CssStories.GapXs} sourceState='none' />

### Small

<Canvas of={CssStories.GapSm} sourceState='none' />

### Medium

<Canvas of={CssStories.GapMd} sourceState='none' />

### Large

<Canvas of={CssStories.GapLg} sourceState='none' />

### Extra large

<Canvas of={CssStories.GapXl} sourceState='none' />

## Choose an implementation

Pick a page in the sidebar under **Wrappers → Stack**, or jump directly:

- **React** — [`@rgrmdesign/rgrm-ds-react`](?path=/docs/wrappers-stack-react--documentation) for React apps
- **CSS** — [`@rgrmdesign/rgrm-ds-css`](?path=/docs/wrappers-stack-css--documentation) for hand-written HTML or any stack without a wrapper
- **Web component** — [`@rgrmdesign/rgrm-ds-elements`](?path=/docs/wrappers-stack-element--documentation) for vanilla JS, server-rendered HTML, or non-React frameworks
