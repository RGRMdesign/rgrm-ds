import { Meta, Canvas } from '@storybook/addon-docs/blocks';

import { FIGMA_LIBRARY_URL } from '../docs/links';
import * as CssStories from './Divider.css.stories';

<Meta title='Content/Divider/Overview' name='Overview' />

# Divider

Horizontal content separator for the RGRM design system. Visual styles live in
`@rgrmdesign/rgrm-ds-css`; React and the web component are thin wrappers that apply
the same `rgrm-` classes.

Load `@rgrmdesign/rgrm-ds-tokens` once in your app so the CSS custom properties are
defined.

**Design spec** — <a href={FIGMA_LIBRARY_URL}>View in Figma</a> for anatomy, variants,
and usage notes.

## Preview

<Canvas of={CssStories.Default} sourceState='none' />

## Choose an implementation

Pick a page in the sidebar under **Content → Divider**, or jump directly:

- **React** — [`@rgrmdesign/rgrm-ds-react`](?path=/docs/content-divider-react--documentation) for React apps
- **CSS** — [`@rgrmdesign/rgrm-ds-css`](?path=/docs/content-divider-css--documentation) for hand-written HTML or any stack without a wrapper
- **Web component** — [`@rgrmdesign/rgrm-ds-elements`](?path=/docs/content-divider-web-components--documentation) for vanilla JS, server-rendered HTML, or non-React frameworks
