import { Meta, Canvas, Source, ArgTypes } from '@storybook/addon-docs/blocks';

import { headingCssClassArgNames } from './fixtures';
import * as CssStories from './Heading.css.stories';
import { headingCssInstallSnippet, headingCssSetupSnippet } from './snippets';

<Meta of={CssStories} name='Documentation' />

# CSS

Vanilla HTML via `@rgrmdesign/rgrm-ds-css/heading`. Apply `.rgrm-heading` together with
an appearance modifier (`.rgrm-heading--h1` … `.rgrm-heading--h6`, or
`.rgrm-heading--display`). Pick the semantic `<h1>`–`<h6>` element independently of the
appearance class.

Use `@rgrmdesign/rgrm-ds-react` or `@rgrmdesign/rgrm-ds-elements` when you prefer a
framework wrapper that applies these classes for you.

## Install

<Source language='bash' code={headingCssInstallSnippet()} />

## Setup

Import tokens and the stylesheet once in your app entry.

<Source language='tsx' code={headingCssSetupSnippet()} />

## Classes

<ArgTypes of={CssStories} include={headingCssClassArgNames} />

## Display

<Canvas of={CssStories.Display} />

## Appearance 1

<Canvas of={CssStories.H1} />

## Appearance 2

<Canvas of={CssStories.H2} />

## Appearance 3

<Canvas of={CssStories.H3} />

## Appearance 4

<Canvas of={CssStories.H4} />

## Appearance 5

<Canvas of={CssStories.H5} />

## Appearance 6

<Canvas of={CssStories.H6} />
