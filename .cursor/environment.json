{
  "build": {
    "dockerfile": "Dockerfile",
    "context": "."
  },
  "install": "corepack enable && corepack prepare pnpm@10.33.0 --activate && pnpm install --frozen-lockfile",
  "start": "pnpm build:packages",
  "terminals": [
    {
      "name": "storybook",
      "command": "pnpm storybook -- --ci"
    }
  ]
}
